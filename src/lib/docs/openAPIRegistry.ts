// Import the OpenAPIRegistry class from the '@asteasolutions/zod-to-openapi' package
import { OpenAPIRegistry } from '@asteasolutions/zod-to-openapi';

// Create a new instance of OpenAPIRegistry
const registry = new OpenAPIRegistry();

// Export the registry instance as the default export of this module
export default registry;